import "axion";

declare module "axios" {
    //export interface AxiosRequestConfig {
        //get<T>(url:string, config?:AxiosRequestConfig):Promise<T>;
        //post<T, D>(
            //url:string, 
            //data?:D, config?:AxiosRequestConfig<D>,
            //):Promise<T>;
    //}
}